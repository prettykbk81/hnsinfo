anthropic>=0.34.0
httpx>=0.27.0
streamlit>=1.36.0
requests>=2.32.0
faster-whisper>=1.0.0
numpy>=1.24.0
torch>=2.0.0
librosa>=0.10.0

# faster-whisper 라이브 STT는 ffmpeg 실행 파일이 PATH에 있어야 동작합니다.
# winget install --id Gyan.FFmpeg -e --source winget

# 화자 구분(diarization)용 resemblyzer는 의존성 webrtcvad가 Windows에서 빌드 도구 없이는
# 설치 실패하므로 --no-deps로 별도 설치 필요 (proxy.py가 webrtcvad를 스텁 처리해서 실제로는 안 씀).
# pip install resemblyzer --no-deps
